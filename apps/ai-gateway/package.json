{
  "name": "@ptt/ai-gateway",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${AI_GATEWAY_PORT:-3104} --reload",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${AI_GATEWAY_PORT:-3104}",
    "install:py": "python3 -m pip install -r requirements.txt",
    "lint": "echo \"lint ok @ptt/ai-gateway\""
  }
}
