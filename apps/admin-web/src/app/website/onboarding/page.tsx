import PlaceholderPage from '@/components/PlaceholderPage';

export default function Page() {
  return (
    <PlaceholderPage
      title="Website Onboarding"
      mockup="02"
      note="Wizard Brand Kit → Catalog → Theme Match → Payment → Go-live (W3)."
    />
  );
}
