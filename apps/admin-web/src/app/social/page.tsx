import PlaceholderPage from '@/components/PlaceholderPage';

export default function Page() {
  return (
    <PlaceholderPage
      title="Social / Live"
      mockup="10"
      note="Ngoài WebCom thuần — tích hợp sau; route giữ chỗ trong shell."
    />
  );
}
